<%= form_for(@menu, :url => {:action => "create_menu"}) do |f| %>
  <%=hidden_field_tag :restaurant_id, params[:id] %>

    name : <%= f.text_field :menuname %>
    price : <%= f.text_field :price %>
  <%= submit_tag("submit") %>

<% end %>
